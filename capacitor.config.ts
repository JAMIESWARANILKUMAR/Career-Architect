import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.vyntyraconsultancyservices',
  appName: 'SEC 2 PROJECT',
  webDir: 'dist'
};

export default config;
